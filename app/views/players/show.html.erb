<div class="container">
  <h1 class='roboto'>Player description</h1>

  <table class="table">
    <tr>
      <th>ID</th>
      <th>Prénom</th>
      <th>Nom</th>
      <th>email</th>
      <th>Tel</th>
      <th>Password</th>
      <th>edit</th>
      <th>delete</th>
    </tr>
    <tr>
      <th><%= link_to @player.id, player_path(@player) %></th>
      <td><%= @player.first_name %></td>
      <td><%= @player.last_name %></td>
      <td><%= @player.email %></td>
      <td><%= @player.phone_number %></td>
      <td><%= @player.password %></td>
      <td><%= link_to 'edit', edit_player_path(@player) %></td>
      <td><%= link_to 'supp', player_path(@player),
      method: :delete, data: { confirm: "Confime stp" }  %></td>
    </tr>
  </table>
</div>
